import { HttpsrcDirective } from './httpsrc.directive';

describe('HttpsrcDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpsrcDirective();
    expect(directive).toBeTruthy();
  });
});
